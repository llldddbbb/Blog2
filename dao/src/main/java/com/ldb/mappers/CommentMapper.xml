<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 对应dao接口 -->
<mapper namespace="com.ldb.dao.CommentDAO">

    <resultMap id="CommentResultMap" type="com.ldb.pojo.po.CommentPO" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="nickName" property="nickName" jdbcType="VARCHAR" />
        <result column="userIP" property="userIP" jdbcType="VARCHAR" />
        <result column="content" property="content" jdbcType="VARCHAR" />
        <result column="publishTime" property="publishTime" jdbcType="TIMESTAMP" />
        <result column="blogId" property="blogId" jdbcType="INTEGER" />
    </resultMap>

    <sql id="listVO">
        nickName,content,publishTime
    </sql>

    <select id="getCommentCount" parameterType="Integer" resultType="int">
        select count(*) from comment where blogId=#{blogId}
    </select>

    <select id="listNewCommentVO" resultMap="CommentResultMap">
        SELECT <include refid="listVO"/> from comment ORDER BY publishTime DESC limit 0,5
    </select>

    <select id="listCommentPO" resultMap="CommentResultMap">
        select id,<include refid="listVO"/> from comment where blogId=#{blogId} ORDER BY publishTime
    </select>

</mapper> 